{
  "name": "puppeteer-pdf",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "if [ \"$RENDER\" = \"true\" ]; then npx puppeteer browsers install chrome --path=$PUPPETEER_CACHE_DIR; fi",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "puppeteer": "^22.9.0"
  }
}